stages:
  - validate

validate_generated_customizations:
  stage: validate
  image: python:3.12-slim
  script:
    - python3 scripts/sync_haca_customizations.py --check
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
